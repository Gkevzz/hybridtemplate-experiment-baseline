# /docs

- `handoff/` — orientation and boundaries for any AI tool or contributor opening this repo outside Lovable.
- `constitution/` — read-only Markdown snapshots of the HybridBonus constitutional documents, originally exported from Lovable memory (`mem://...`). This template copy is not actively synced with Lovable unless explicitly configured.
